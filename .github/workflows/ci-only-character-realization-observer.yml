name: CI-only Full Regression Bisect

on:
  pull_request:
    branches:
      - develop
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bisect:
    name: full regression grouped diagnostics
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install -r requirements-dev.txt
      - name: Tests a-f
        if: always()
        run: |
          files=$(find tests -type f -name 'test_*.py' | sort | awk -F/ '{n=$NF; c=substr(n,6,1); if (c >= "a" && c <= "f") print $0}')
          test -z "$files" || pytest -x -q $files
      - name: Tests g-l
        if: always()
        run: |
          files=$(find tests -type f -name 'test_*.py' | sort | awk -F/ '{n=$NF; c=substr(n,6,1); if (c >= "g" && c <= "l") print $0}')
          test -z "$files" || pytest -x -q $files
      - name: Tests m-r
        if: always()
        run: |
          files=$(find tests -type f -name 'test_*.py' | sort | awk -F/ '{n=$NF; c=substr(n,6,1); if (c >= "m" && c <= "r") print $0}')
          test -z "$files" || pytest -x -q $files
      - name: Tests s-z
        if: always()
        run: |
          files=$(find tests -type f -name 'test_*.py' | sort | awk -F/ '{n=$NF; c=substr(n,6,1); if (c >= "s" && c <= "z") print $0}')
          test -z "$files" || pytest -x -q $files
